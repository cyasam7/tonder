import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    container: {
        padding: 2,
        backgroundColor: "#fff",
        flexDirection: "row",
    },
    wrapper: {
        flex: 1,
        flexDirection: "row",
        margin: 5,
        alignItems: "center",
    },
    input: {
        flex: 1,
        padding: 10,
    },
});
